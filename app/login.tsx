/**
 * Thin wrapper so Expo Router treats LoginScreen as the /login page.
 * If you later move the real component into this file, just delete the export line
 * and paste the component code here.
 */
export { default } from './auth/LoginScreen';
